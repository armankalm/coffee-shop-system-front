VITE_API_BASE_URL=http://localhost:8080/api

# Stripe publishable (test) key — safe to expose to the browser. Never put the secret key here.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
